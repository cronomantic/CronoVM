# Build artifact produced by build_picolibc.sh (curated picolibc subset, linked
# to one i386-elf bitcode module). Regenerated from the external/picolibc
# submodule + picolibc.h; never committed.
picolibc.bc
# Build artifact produced by build_cxxio.sh (the vendored libc++ iostream/locale
# subset in libcxx-src/, linked to one bitcode module). Regenerated; not committed.
cxxio.bc
# Build artifact produced by build_cxxio.sh (the vendored libc++ std::filesystem
# subset, a separate module linked explicitly by filesystem-using carts). Not committed.
cxxfs.bc
